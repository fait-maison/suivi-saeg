apiVersion: v1
kind: Service
metadata:
  name: {{ include "suivi-saeg.fullname" . }}
  labels:
    {{- include "suivi-saeg.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "suivi-saeg.selectorLabels" . | nindent 4 }}
